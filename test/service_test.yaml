apiVersion: v1
kind: Service
metadata:
  name: fileserver-service
spec:
  selector:
    app: myApp
  ports:
  - name: http
    port: 8080
    targetPort: 6800
  type: ClusterIP
  clusterIP: "10.20.0.2"
